android {
    signingConfigs {
        release {
            storeFile file('keystore/release.jks')
            storePassword 'Epee5Reishohsh'
            keyAlias 'release-key'
            keyPassword 'nooVe9phai7nah'
        }
    }
}